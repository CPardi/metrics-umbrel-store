services:
  web:
    image: quay.io/prometheus/node-exporter:v1.8.2
#    user: 1000:1000
    command:
      - '--path.rootfs=/host'
    network_mode: host
    pid: host
    restart: on-failure
    volumes:
      - '/:/host'
